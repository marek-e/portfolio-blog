/**
 * Registry key for the prefers-reduced-motion flag, evaluated once at game creation
 * (PRD §6.9): when set, scenes skip ambience layers, walk bob, marker/prompt pulses,
 * camera smoothing and the confetti celebration — the world stays fully functional.
 */
export const REDUCED_MOTION_REGISTRY_KEY = 'world-reduced-motion';
